import Link from "next/link";
import type { Locale } from "@/content/site";
import { featuredProductFamilies, productFamilies } from "@/content/product-families";
import { productVariants } from "@/content/product-variants";
import { ProductFamilyCard } from "@/components/products/ProductFamilyCard";

export function FeaturedProductFamilies({ locale }: { locale: Locale }) { return <section className="section featured-products" aria-labelledby="featured-products-title"><div className="section-heading"><p className="kicker">{locale === "ar" ? "اختيارات من الكتالوج" : "FROM THE CATALOG"}</p><h2 id="featured-products-title">{locale === "ar" ? "عائلات مختارة لكل مساحة" : "Selected families for every space"}</h2><p>{locale === "ar" ? `${featuredProductFamilies.length} عائلات من إجمالي ${productFamilies.length} عائلة و${productVariants.length} موديل. السعر الحالي متاح عند الطلب.` : `${featuredProductFamilies.length} families from ${productFamilies.length} families and ${productVariants.length} variants. Current pricing is available on request.`}</p></div><div className="product-grid featured-grid">{featuredProductFamilies.map((family) => <ProductFamilyCard key={family.id} family={family} variants={productVariants.filter((variant) => variant.familyId === family.id)} locale={locale} />)}</div><Link className="catalog-link" href={`/${locale}/products`} prefetch={false}>{locale === "ar" ? "عرض الكتالوج" : "View catalog"}<span>↗</span></Link></section>; }
